# simple-javascript-function-testing
This example demonstrates fundamental software testing principles within a web context. It features a simple JavaScript function (`calculateDiscount`) that simulates a common web application logic. The code includes a basic, self-contained testing framework to verify the function's correct behavior with various inputs and its error handling for inv
